; (library
;  (name args)
;  (modules args)
;  (libraries cmdliner fmt.tty fmt.cli logs.fmt logs.cli))
; 
; (executable
;  (name info)
;  (public_name uniq.info)
;  (modules info)
;  (libraries args uniq.mod))
; 
; (executable
;  (name qualify)
;  (public_name uniq.qualify)
;  (modules qualify)
;  (libraries args uniq.mod uniq.cfg))
; 
; (executable
;  (name resolve)
;  (public_name uniq.resolve)
;  (modules resolve)
;  (libraries args uniq.mod uniq.cfg uniq.meta))
; 
; (executable
;  (name meta)
;  (public_name uniq.meta)
;  (modules meta)
;  (libraries args uniq.meta))
; 
; (executable
;  (name digest)
;  (public_name uniq.digest)
;  (modules digest)
;  (libraries cmdliner args uniq.mod))

(executable
 (name unique)
 (modules unique)
 (public_name uniq)
 (libraries compiler-libs.common uniq.solver uniq.cli))

(library
 (name uniq_cli)
 (modules uniq_cli)
 (public_name uniq.cli)
 (wrapped false)
 (libraries
  compiler-libs.common
  uniq.cfg
  uniq.meta
  uniq.policy
  re
  bos
  fpath
  fmt.tty
  fmt.cli
  logs.fmt
  logs.cli))
